# secrets - never commit
tools/llm_config.json

# other people's data - re-downloadable from sts2runs.com
external_data/

# run artefacts - regenerated by running the code
trajectories/
*.pkl
*.pt

# C# build output (api_probe is rebuilt with `dotnet build`)
tools/api_probe/bin/
tools/api_probe/obj/

# python caches / build output
*.egg-info/
__pycache__/
*.py[cod]
.pytest_cache/

#Claude prompts
CLAUDE.md
